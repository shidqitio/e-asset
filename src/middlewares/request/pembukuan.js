// const {check} = require('express-validator')


// exports.store = (req, res, next  ) => {
//     if(req.body.catat === "DBR" || req.body.catat === "DBL") {
//         console.log("Tes", check)
//         // check("kode_asset").notEmpty().withMessage("Kode Asset Harus Diisi")
//         // check("no_sppa").notEmpty().withMessage("No Sppa Harus Diisi")
//     }
// }
